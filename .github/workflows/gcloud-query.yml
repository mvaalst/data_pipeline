name: Google Cloud Query

on:
  push:
    branches:
      - main

jobs:
  run-query:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Google Cloud
      uses: 'google-github-actions/auth@v2'
      with:
        credentials_json: '${{ secrets.GOOGLE_CLOUD_CREDENTIALS }}'
        
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install google-cloud-bigquery google-auth

    - name: Authenticate to Google Cloud
      run: echo "${{ secrets.GOOGLE_CLOUD_CREDENTIALS }}" > $HOME/gcloud-credentials.json

    - name: Initialize BigQuery Client
      run: |
        # Set the GOOGLE_APPLICATION_CREDENTIALS environment variable to the credentials file
        export GOOGLE_APPLICATION_CREDENTIALS=$HOME/gcloud-credentials.json
        
        # Now initialize the BigQuery client in the Python script
        python -c "
        from google.cloud import bigquery
        
        # Initialize the BigQuery client
        client = bigquery.Client()
        print('BigQuery client initialized.')
        "
